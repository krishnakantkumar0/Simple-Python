#offers on chhocolate
#enter the num find in list else output will closest num 
print ('hi krishnakant welcome in python')
def offer(a, b):
	if (choc < wrap)
        return 0;
 
    newChoc = choc/wrap;
 
#Now we have "newChoc + choc%wrap" wrappers.
    return newChoc + countRec(newChoc + choc%wrap,
                              wrap);
a=int(input('how much money in your pocket'))
b=int(input('price of chocolate'))
print('OFFER of CHOCOLATE', a,"and", b,"is", offer(a, b))

